[build]
  command = "npm install --prefix frontend && npm run build --prefix frontend"
  publish = "frontend/dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev --prefix frontend"
  port = 8888
  targetPort = 5173
  publish = "frontend/dist"

[functions]
  included_files = ["documents/data/raw/**"]
